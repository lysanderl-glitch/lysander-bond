---
import Layout from '../../layouts/Layout.astro';
---

<Layout darkContent={true} title="AI团队的决策边界：什么时候该授权Agent自主决策 - Lysander" description="从'每次都来问我'到'专业的事专业的人做'——一个实际使用Claude Code团队的授权演进">
  <article class="max-w-4xl mx-auto px-6 py-16">
    <header class="mb-12">
      <div class="flex items-center gap-3 mb-4">
        <time class="text-white/40 text-sm">2026-04-27</time>
        <div class="flex gap-2">
          <span class="px-2 py-0.5 bg-purple-500/20 text-purple-400 rounded text-xs">问题日志</span>
          <span class="px-2 py-0.5 bg-purple-500/20 text-purple-400 rounded text-xs">AI工程</span>
          <span class="px-2 py-0.5 bg-blue-500/20 text-blue-400 rounded text-xs">Synapse</span>
        </div>
      </div>
      <h1 class="text-4xl font-bold text-white">AI团队的决策边界：什么时候该授权Agent自主决策</h1>
    </header>

    <div class="prose prose-invert max-w-none space-y-8 text-white/80 leading-relaxed">

<h2>AI团队的决策边界：什么时候该授权Agent自主决策</h2>

<p>三个月前，我们团队的Claude Code使用模式是这样的：每隔几分钟，就会有人喊一声"它又来问我要不要继续了"。Agent写到一半停下来，等待确认。我们确认，它继续，写几行，又停。那段时间我有一种强烈的感觉——我们花了大价钱买了一个非常能干的实习生，但每隔五分钟就要签一次字，效率还不如自己写。这不是AI的问题，是我们还没想清楚授权边界在哪里。</p>

<p>这个问题的根源在于：大多数团队使用AI的方式，本质上还是"高级搜索引擎+代码补全"。一问一答，每次交互都是独立的。Agent跑起来做复杂任务时，团队的本能反应是缩小它的自主权，遇到任何不确定的地方都让它停下来问人。这种模式的代价是隐性的——你觉得安全，但实际上你把AI拖回了最低效的使用方式。真正的问题不是"要不要让Agent自主决策"，而是"哪些决策适合交给它，哪些必须留给人"。这是两个完全不同的问题，混在一起就会陷入要么过度干预、要么失控的两个极端。</p>

<h2>我们踩过的坑：授权太宽和太窄都会出问题</h2>

<p>第一个阶段，我们走的是另一个极端——给Agent开了很大的权限，让它自己跑完整个任务链。结果某次重构任务，它把一个内部工具库的接口改了，下游三个服务都没注意到，直到联调的时候才炸。不是Agent写错了，代码逻辑没问题，但它不知道这个改动的影响范围超出了代码库本身，涉及到另一个团队正在开发的功能。这是一个典型的"技术上正确，工程上灾难"的案例。之后我们矫枉过正，加了一堆确认节点，又回到了开头那个"每隔五分钟停一次"的状态。</p>

<p>我们花了将近两个月，才摸出一套相对稳定的分类框架。核心思路是把决策按两个维度切分：<strong>可逆性</strong>和<strong>影响范围</strong>。可逆性高、影响范围小的决策（比如函数命名、注释风格、单元测试的写法）——直接授权，让Agent自己定，不用问。可逆性低、影响范围大的决策（比如数据库schema变更、对外接口的破坏性修改、依赖版本的大版本升级）——必须停下来，走人工审批。中间地带最难处理：可逆性中等、影响范围不确定的决策，我们的做法是让Agent先生成一个"变更影响说明"，用自然语言描述它打算做什么、预期影响哪些模块，然后由人快速判断要不要继续，而不是让它去问"我可以继续吗"这种没有上下文的问题。</p>

<h2>具体怎么落地：三个让授权机制真正运转起来的实践</h2>

<p>第一个实践是<strong>显式的任务边界声明</strong>。我们现在给Agent的任务描述里，会加一段"禁止自主决策的操作清单"，格式非常直白：不能修改`config/production`目录下的任何文件，不能删除超过50行的代码块，不能新增外部依赖。这不是不信任Agent，而是把工程上的约束提前告诉它，而不是等它踩到才停下来。Claude Code在这方面的表现让我意外——给了清晰的边界声明之后，它在边界内的自主性反而更强，因为它不需要在每个操作上猜测"这个我能不能做"。</p>

<p>第二个实践是<strong>区分"确认需求"和"信息需求"</strong>。Agent停下来问的问题分两种：一种是"我需要你批准这个操作"，一种是"我缺少信息，无法继续"。前者可以通过授权边界消除大部分，后者是真实的阻塞。我们做了一个小改造，要求Agent在停下来之前，必须先声明是哪种情况，并且信息需求类的问题必须具体到"我需要知道X，因为不知道这个我无法完成Y"。这个改造之后，无效的中断请求减少了大约60%。</p>

<p>第三个实践是<strong>建立决策日志而不是决策审批</strong>。对于中等风险的决策，与其让Agent等待批准，不如让它先执行、同步记录决策理由，人在事后做异步审查。这需要团队有一定的风险容忍度，但好处是Agent的工作不会因为人的响应延迟而卡住，而人也能从一堆决策日志里发现模式问题，而不是淹没在逐条审批里。我们用一个简单的markdown文件做决策日志，每次Agent做了中等风险操作就追加一条记录，包括：操作内容、理由、预期影响、实际影响（事后填）。</p>

<h2>一个不太舒服的结论</h2>

<p>做完这些之后，我意识到授权边界的问题，本质上是团队自身对工程风险的认知问题。Agent的决策能力其实比我们想象的更强，但它不知道你们团队的隐性约定、历史包袱和当前的业务优先级。授权机制建不起来，很多时候不是AI不够聪明，而是团队自己没有把这些隐性知识显式化。你要先能回答"什么决策人来做也不需要讨论"，才能回答"什么决策可以交给Agent"。这个过程逼着我们梳理了很多一直模糊的工程规范，反而是个意外收获。</p>

<p>可复用的原则总结一下：用可逆性和影响范围做决策分类；边界声明要比权限收紧更有效；区分确认需求和信息需求；对中等风险决策用日志替代审批；授权机制的建设过程本身就是工程知识显式化的过程。</p>

<p>如果你在构建AI工程团队，欢迎参考我们开源的 Synapse 框架——我们把上面这套授权分类机制和决策日志的实现都沉淀在里面了，省去一些重复踩坑的时间。</p>

    </div>

    <footer class="mt-16 pt-8 border-t border-white/10">
      <p class="text-white/40 text-sm mb-4">
        如果你在构建 AI 工程团队，欢迎参考我们开源的
        <a href="https://github.com/lysanderl-glitch/synapse" class="text-sky-400 hover:text-sky-300" target="_blank" rel="noopener noreferrer">Synapse 框架</a>
      </p>
      <a href="/blog" class="text-sky-400 hover:text-sky-300">&larr; 返回博客</a>
    </footer>
  </article>
</Layout>
