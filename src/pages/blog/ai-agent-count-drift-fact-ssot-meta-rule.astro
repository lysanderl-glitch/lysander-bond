---
import Layout from '../../layouts/Layout.astro';
---

<Layout darkContent={true} title="当 AI 团队的人数出现三个版本：我们如何用元规则修复数字漂移 - Lysander" description="从同一系统出现44/46/50三个agent人数的真实事故出发，剖析多文件维护场景下数字漂移的根因并提出Fact-SSOT元规则">
  <article class="max-w-4xl mx-auto px-6 py-16">
    <header class="mb-12">
      <div class="flex items-center gap-3 mb-4">
        <time class="text-white/40 text-sm">2026-04-26</time>
        <div class="flex gap-2">
          <span class="px-2 py-0.5 bg-purple-500/20 text-purple-400 rounded text-xs">问题日志</span>
          <span class="px-2 py-0.5 bg-purple-500/20 text-purple-400 rounded text-xs">AI工程</span>
          <span class="px-2 py-0.5 bg-blue-500/20 text-blue-400 rounded text-xs">Synapse</span>
        </div>
      </div>
      <h1 class="text-4xl font-bold text-white">当 AI 团队的人数出现三个版本：我们如何用元规则修复数字漂移</h1>
    </header>

    <div class="prose prose-invert max-w-none space-y-8 text-white/80 leading-relaxed">

<h2>当 AI 团队的人数出现三个版本：我们如何用元规则修复数字漂移</h2>

<p>上周我们的系统里同时存在三个"正确答案"：44、46、50。这三个数字都是我们 AI agent 团队的成员人数，分别来自三个不同的文档文件。没有人故意制造这个混乱——每次更新都是善意的，每次改动都有理由，但最终结果是系统对自身的基本事实失去了共识。这不是一个哲学问题，是一个工程问题。</p>

<h2>事故复盘：三个数字是怎么来的</h2>

<p>我们的 agent 系统维护着几类文档：系统提示文件、团队配置表、运行日志摘要。当团队规模从 44 人扩展时，有人更新了系统提示，写成了 46；另一个人基于不同的统计口径（把兼职 agent 也算进去）在配置表里写了 50；原始的 44 还留在日志摘要的历史快照里没被清理。三个文件，三种"真相"，互不干扰地共存着，直到一个需要跨文件做决策的任务把它们全部拉进了同一个上下文窗口。</p>

<p>AI 在处理这个任务时的行为很有意思：它没有报错，而是悄悄地选择了其中一个数字继续执行。这比报错更危险，因为你不知道它选了哪个，也不知道选择依据是什么。我们后来复现了几次，发现结果不稳定——有时用 46，有时用 50，取决于文档在上下文里的排列顺序。这就是数字漂移的典型形态：不是一次性的错误，而是持续的、不可预测的偏差。</p>

<h2>根因分析：多文件维护的结构性缺陷</h2>

<p>问题的根因不在于某次手误，而在于我们的文档体系缺乏对"事实性数据"的专门管理机制。在代码工程里，我们有 Single Source of Truth（单一事实来源）的概念——一个常量只在一个地方定义，其他地方引用它。但在 AI 系统的知识库和配置文档里，我们习惯于在每个文件里把相关信息写完整，这导致同一个事实被反复陈述，每次陈述都可能产生微小偏差。</p>

<p>更深层的问题是：这些文档是给 AI 读的，不是给人读的。人类读者在遇到数字不一致时会停下来质疑，会去找原始依据；AI 在大多数情况下会默默消化矛盾，用某种内部权重决定相信哪个版本，然后继续执行。这意味着人类文档管理里的小毛病，在 AI 系统里会被放大成系统性风险。</p>

<h2>解法：Fact-SSOT 元规则</h2>

<p>我们引入了一个叫 Fact-SSOT（Fact Single Source of Truth）的元规则，逻辑很简单，分三条：</p>

<p><strong>第一条，事实隔离。</strong>把所有会变化的定量事实（人数、版本号、日期、阈值等）从叙述性文档里抽出来，集中存放在一个专用的事实注册表（Fact Registry）里。这个注册表是纯数据的，没有叙述，没有解释，只有键值对。其他所有文档在需要引用这些数字时，使用明确的引用标记而不是直接写死数值。</p>

<p><strong>第二条，写入权限收窄。</strong>事实注册表设定明确的写入规则：谁有权修改、修改时需要同步通知哪些下游文档、修改后需要留存变更记录。这不需要复杂的工具，一个有版本控制的 YAML 文件加上 Git commit 规范就够了。关键是让"修改事实"这个行为变得有仪式感，而不是顺手改一下就过去了。</p>

<p><strong>第三条，AI 读取时的冲突检测。</strong>在系统提示里加入一条元指令：当 AI 在上下文中检测到同一实体存在不一致的定量描述时，必须停止执行，输出冲突报告，等待人工确认。这条规则把"悄悄选择"变成了"显式报错"，把隐性风险变成了可见的异常。</p>

<h2>实施效果与边界</h2>

<p>我们把这套规则落地大概花了半天：迁移现有文档用了两小时，写事实注册表用了一小时，更新系统提示用了半小时。落地后运行了两周，冲突检测触发了三次，每次都是真实的数据不一致被及时发现，没有一次误报。更重要的是，团队在更新数字时开始有意识地先改注册表，这个习惯本身就是收益。</p>

<p>需要说清楚的是，Fact-SSOT 不是银弹。它解决的是结构性不一致，不解决数据本身的准确性问题——如果注册表里的数字一开始就是错的，这套规则帮不了你。它也不适合高频实时变化的数据，那种场景需要真正的数据库和 API，而不是文档规范。Fact-SSOT 的适用范围是：变化频率以天或周计、需要被多个 AI 任务共同引用的配置型事实。</p>

<h2>可复用的原则</h2>

<p>如果你的团队也在维护多文件的 AI 知识库或配置体系，我建议把这件事当作一个工程问题而不是管理问题来处理。不要依赖"大家下次注意"——注意力是不可靠的资源。把事实数据从叙述文档里物理隔离出来，让引用关系显式化，让冲突检测自动化。这三件事不需要复杂工具，需要的是把"文档一致性"当作一等公民来设计，而不是事后校对的工作。数字漂移是个小问题，但它告诉你系统对自身事实的掌控力有多脆弱——在 AI 大量介入决策的系统里，这个脆弱性值得认真对待。</p>

<p>如果你在构建 AI 工程团队，欢迎参考我们开源的 Synapse 框架——我们把 Fact-SSOT 以及其他在实际运营中沉淀下来的元规则都集成在里面了，可以直接拿来用，也可以作为你自己体系的参考起点。</p>

    </div>

    <footer class="mt-16 pt-8 border-t border-white/10">
      <p class="text-white/40 text-sm mb-4">
        如果你在构建 AI 工程团队，欢迎参考我们开源的
        <a href="https://github.com/lysanderl-glitch/synapse" class="text-sky-400 hover:text-sky-300" target="_blank" rel="noopener noreferrer">Synapse 框架</a>
      </p>
      <a href="/blog" class="text-sky-400 hover:text-sky-300">&larr; 返回博客</a>
    </footer>
  </article>
</Layout>
