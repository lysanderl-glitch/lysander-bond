---
// Chinese blog index — dynamic via Content Collections.
//
// Lists every post in the `blog` collection where lang === 'zh'.
// Uses the typed Content Collection so future ZH posts appear automatically.
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';

const allZhPosts = await getCollection('blog', ({ data }) => data.lang === 'zh');
const posts = allZhPosts.sort(
  (a, b) => b.data.publishDate.getTime() - a.data.publishDate.getTime()
);

const formatDate = (d: Date) =>
  `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`;
---

<Layout darkContent={true} title="博客 - Lysander" description="关于 AI 工具、Claude Code 使用心得、技术探索">
  <Fragment slot="head">
    <meta name="x-has-english" content="true" />
  </Fragment>
  <section class="max-w-4xl mx-auto px-6 py-16">
    <h1 class="text-4xl font-bold text-white mb-4">博客</h1>
    <p class="text-white/60 mb-12">关于 AI 工具、Claude Code 使用心得、技术探索</p>

    <div class="space-y-6">
      {posts.map((post) => (
        <a href={`/blog/${post.data.slug}`} class="group block p-6 bg-white/5 rounded-xl border border-white/10 hover:border-sky-500/50 transition-colors">
          <div class="flex items-center gap-3 mb-3 flex-wrap">
            <time class="text-white/40 text-sm">{formatDate(post.data.publishDate)}</time>
            {post.data.priority && (
              <span class="px-2 py-0.5 bg-purple-500/20 text-purple-400 rounded text-xs">{post.data.priority}</span>
            )}
            {post.data.pillar && (
              <span class="px-2 py-0.5 bg-blue-500/20 text-blue-400 rounded text-xs">{post.data.pillar}</span>
            )}
          </div>
          <h2 class="text-xl font-semibold text-white group-hover:text-sky-400 transition-colors">
            {post.data.title}
          </h2>
          <p class="text-white/50 mt-2">{post.data.description}</p>
        </a>
      ))}
    </div>

    <div class="mt-12 pt-8 border-t border-white/10 text-center text-sm text-white/50">
      如果你在构建 AI 工程团队，我们开源了 Synapse 框架 →
      <a href="https://github.com/lysanderl-glitch/synapse"
         class="text-sky-400 hover:text-sky-300 ml-1"
         target="_blank" rel="noopener noreferrer">
        synapse-core on GitHub
      </a>
    </div>
  </section>
</Layout>
